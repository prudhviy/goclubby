TO DO -

	gzip/deflate compression
	virtual hosts support
	closure/JSMIN minification support
	set headers support - content-length, content-encoding etc..
	global server config and mapping config(per file content-encoding)
	server mode(in-memory cache) and development mode

low priority -
	
	all configs to be shown on http://127.0.0.1:8000/config/

